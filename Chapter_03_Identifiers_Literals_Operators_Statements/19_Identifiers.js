//let class = "Hello Worls";
//class is a reserved keyword and cannot be used as an identifier. This will result in a syntax error. To fix this, you can choose a different name for the variable, such as "myClass" or "className". For example:
 
//let if = true; 
//if is a reserved keyword and cannot be used as an identifier. This will result in a syntax error. To fix this, you can choose a different name for the variable, such as "isIf" or "ifCondition". For example:

//let return = 5;
//return is a reserved keyword and cannot be used as an identifier. This will result in a syntax error. To fix this, you can choose a different name for the variable, such as "myReturn" or "returnValue". For example:
 
//let myVariable = 10;
// This is a valid identifier can be used without any issue.

// they look like reserved keywords but they are not.
//let undefined = 5; // This works but Never do this. It can lead to confusion and bugs in your code.
// let infinity = 10; // This works but Never do this. It can lead to confusion and bugs in your code.

