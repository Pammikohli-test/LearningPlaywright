let count= 34;
let nagative= -23;
let zero = 0;

let h = 0xFF;
let color_hex = 0xFF0000; // In wed automation for store the colour 
console.log(color_hex);

let octal =0o77;
let pi = 3.14159;

let million = 1e6;
let tiny = 1.5e6-4;

//single quotes
let single = 'Hello World';
let withdouble = 'She said "hi"';

//Double Quotes
let double = "Hello World";
let withsingle ="It's a test";

//Template literal
let name = "Pramaod";
let full_name =`Hi, ${name} dutta`;
console.log(full_name);

let math =`2+2 = ${2 + 2}`; //2+2- value = ${2 + 2}-expressions
console.log(math);

// I want to store the path of the file
let path = "C:\\Users\\Pammi\\file.txt";
// single slash is not allowed as it is '/' slash is special character in java so we have use extra slash
console.log(path);

// -----> // - for URL's
// -----> \\ - for Path

let address ="https://app.vwo.com/#login";
console.log(address);

//String literals edge cases
let empty = "";      // empty string (falsy!)
let space = " ";     // space string (truely!)
let zero1 ="0";      // string zero (truely!)

let isLoggedIn = true;
let hasPermission = false;



