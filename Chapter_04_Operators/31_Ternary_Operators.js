let age = 20;
//let age = 17;

let is_pramod_will_go_goa = age > 18 ? "yes, let go to goa!" : "No, you are minor, not going";
console.log(is_pramod_will_go_goa);

// Ternary = condition ? "true result" : "false result"